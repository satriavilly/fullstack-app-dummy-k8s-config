apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: myapp
data:
  DATABASE_HOST: "postgres"
  DATABASE_PORT: "5432"
  DATABASE_NAME: "myapp_db"
  NODE_ENV: "production"
  NEXT_PUBLIC_API_URL: "http://backend:8080/api"
